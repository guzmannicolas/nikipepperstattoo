// Minimal configuration for astro-i18next CLI
module.exports = {
  // Locales available in the project
  locales: ['en', 'es'],
  // Default locale
  defaultLocale: 'en',
  // Whether to generate pages for the default locale without a prefix
  showDefaultLocale: false,
  // Optional route mappings; keep empty for default behavior
  routes: {}
};

---
import HeroSectionLogic from "./HeroSectionLogic";


const { lang, t } = Astro.props;
---

<HeroSectionLogic
  client:load
  title="Transform your space into a botanical beauty."
  subtitle={t("home.subtitle")}
  ctaPrimary={t("home.viewWorks")}
  ctaSecondary={t("home.bookConsultation")}
  lang={lang}
/>
